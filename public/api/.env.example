# =============================================================================
# ENVIRONMENT CONFIGURATION
# =============================================================================
#
# ANLEITUNG:
# 1. Kopiere diese Datei zu ".env" (ohne .example)
# 2. Fülle alle Werte mit deinen echten Daten aus
# 3. WICHTIG: .env NIEMALS in Git committen!
#
# PLATZIERUNG AUF DEM SERVER:
# Die .env-Datei kann an folgenden Orten liegen (wird automatisch gefunden):
# - /public/api/.env (gleicher Ordner wie diese Datei)
# - /public/.env (ein Level höher)
# - /.env (Projekt-Root)
# - [DOCUMENT_ROOT]/.env
# - [DOCUMENT_ROOT]/api/.env
#
# =============================================================================

# -----------------------------------------------------------------------------
# ZOOM MEETING API (Server-to-Server OAuth)
# -----------------------------------------------------------------------------
# Erstelle eine Server-to-Server OAuth App in:
# https://marketplace.zoom.us/develop/create
#
# Benötigte Scopes:
# - meeting:write:admin
# - meeting:read:admin
# - user:read:admin

ZOOM_ACCOUNT_ID=dein_account_id_hier
ZOOM_CLIENT_ID=dein_client_id_hier
ZOOM_CLIENT_SECRET=dein_client_secret_hier

# -----------------------------------------------------------------------------
# E-MAIL KONFIGURATION (IONOS SMTP)
# -----------------------------------------------------------------------------
# WICHTIG: Verwendet PHPMailer mit SMTP-Authentifizierung für IONOS
#
# Admin E-Mail (erhält Benachrichtigungen über neue Buchungen)
ADMIN_EMAIL=steffi@wohlfühlgesundheit.de

# Absender-E-Mail (für ausgehende E-Mails)
# ⚠️ MUSS eine echte IONOS-Mailbox sein!
FROM_EMAIL=steffi@wohlfühlgesundheit.de

# Absender-Name
FROM_NAME=Wohlfühlgesundheit - Holistische Darmtherapie

# -----------------------------------------------------------------------------
# SMTP KONFIGURATION (IONOS)
# -----------------------------------------------------------------------------
# IONOS SMTP Server Settings
# Dokumentation: https://www.ionos.de/hilfe/e-mail/e-mail-programme-einrichten-und-e-mail-abrufen/smtp-und-pop3-einstellungen-fuer-e-mail-programme/

# SMTP Host (IONOS)
SMTP_HOST=smtp.ionos.de

# SMTP Port (587 für TLS, 465 für SSL)
SMTP_PORT=587

# SMTP Verschlüsselung (tls oder ssl)
SMTP_ENCRYPTION=tls

# SMTP Benutzername (deine vollständige E-Mail-Adresse)
SMTP_USERNAME=steffi@wohlfühlgesundheit.de

# SMTP Passwort (das Passwort deiner E-Mail-Adresse)
SMTP_PASSWORD=dein_email_passwort_hier

# -----------------------------------------------------------------------------
# SICHERHEIT
# -----------------------------------------------------------------------------
# Erlaubte Domains für CORS (kommagetrennt)
# WICHTIG: Domain verwendet Umlaut (IDN - Internationalisierte Domain)
# Beide Varianten angeben für maximale Browser-Kompatibilität:
# - UTF-8:    wohlfühlgesundheit.de
# - Punycode: xn--wohlfhlgesundheit-62b.de
# Manche Browser senden die eine, andere die andere Variante!
ALLOWED_ORIGINS=https://wohlfühlgesundheit.de,https://xn--wohlfhlgesundheit-62b.de,https://www.wohlfühlgesundheit.de,https://www.xn--wohlfhlgesundheit-62b.de

# CSRF Secret (zufälliger String, mindestens 32 Zeichen)
# Generiere mit: openssl rand -base64 32
CSRF_SECRET=ändere_dies_zu_einem_zufälligen_string_mindestens_32_zeichen

# -----------------------------------------------------------------------------
# RATE LIMITING
# -----------------------------------------------------------------------------
# Maximale Anzahl Requests pro Zeitfenster
RATE_LIMIT_MAX_REQUESTS=5

# Zeitfenster in Sekunden (3600 = 1 Stunde)
RATE_LIMIT_TIME_WINDOW=3600

# -----------------------------------------------------------------------------
# DEBUG (nur für Entwicklung!)
# -----------------------------------------------------------------------------
# WICHTIG: Auf Production immer auf 'false' setzen!
DEBUG_MODE=false
